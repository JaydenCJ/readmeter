# Contributing to driftwood

This file exists so the README's CONTRIBUTING link resolves — it is part of
readmeter's bundled "good README" example project.

Run `npm test` before opening a pull request.
