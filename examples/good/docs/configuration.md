# driftwood configuration

This file exists so the README's documentation link resolves — it is part
of readmeter's bundled "good README" example project.

| Key | Default | Effect |
|---|---|---|
| `archiveDir` | `~/driftwood` | Where snapshots are stored |
| `timeoutMs` | `10000` | Per-page fetch timeout |
